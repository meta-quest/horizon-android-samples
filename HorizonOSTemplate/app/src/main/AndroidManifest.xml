<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright (c) Meta Platforms, Inc. and affiliates.
     This source code is licensed under the MIT license found in the
     LICENSE file in the root directory of this source tree. -->
<manifest
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:horizonos="http://schemas.horizonos/sdk"
  android:versionCode="1"
  android:versionName="1.0"
  android:installLocation="auto"
>

  <!-- Horizon OS SDK levels are separate from Android SDK levels. The minimum
       keeps the app installable on older releases, while the target opts the
       app into Horizon OS behavior through v207. -->
  <horizonos:uses-horizonos-sdk
    horizonos:minSdkVersion="69"
    horizonos:targetSdkVersion="207"
  />
  <uses-permission android:name="android.permission.INTERNET" />
  <application
    android:allowBackup="false"
    android:label="HorizonOSTemplate"
    android:theme="@style/Theme.AppTheme"
  >
    <!-- Advertise the headset families supported by this starter project. -->
    <meta-data
      android:name="com.oculus.supportedDevices"
      android:value="quest2|questpro|quest3|quest3s"
    />
    <!-- Keep this activity alive while the Horizon OS panel is resized or its
         input configuration changes. -->
    <activity
      android:name="com.meta.horizon.samples.horizonostemplate.MainActivity"
      android:launchMode="singleTask"
      android:configChanges="screenSize|screenLayout|orientation|keyboardHidden|keyboard|navigation|uiMode"
      android:exported="true"
    >
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <!-- Initial panel dimensions. The UI must remain responsive when the user
           resizes the panel. -->
      <layout
        android:defaultHeight="640dp"
        android:defaultWidth="1024dp" />
    </activity>
  </application>
</manifest>
